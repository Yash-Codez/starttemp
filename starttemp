import java.util.*;
public class solution{
    public static void main(String[]args){
        Scanner sc=new Scanner(System.in);
        int t=sc.nextInt();
        while(t-->0){
            int n=sc.nextInt();
            int b=sc.nextInt();
            int x=sc.nextInt();
            int[] nums=new int[n];

            for(int i=0;i<n;i++){
            nums[i]=sc.nextInt();
        }
    }
}
//sort irrespectiveofsign

        Arrays.sort(arr, (a, b) -> {
            int absA = Math.abs(a);
            int absB = Math.abs(b);
            return absA - absB;
        });
//check intersection of set
 
            // Find intersection
            queenSet.retainAll(kingSet);

            // Output intersection size
            System.out.println(queenSet.size());
        }

        // TreeMap to keep sorted order of keys
        Map<Integer, List<Integer>> map = new TreeMap<>(); 

        for (int i = 0; i < arr1.length; i++) {
            map.putIfAbsent(arr1[i], new ArrayList<>());
            map.get(arr1[i]).add(arr2[i]);
        }
